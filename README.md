# ZestyBackendInterview

---


### Usage:
1. SSH into the `access-machine`
2. run the python script:
```shell
cd $HOME/ZestyBackEndInterview && source setup.sh;
.venv/bin/python3 list_instances.py --role-arn arn:aws:iam::577926974532:role/CrossAccountEc2ListInstancesRole
```
